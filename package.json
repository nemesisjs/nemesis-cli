{
  "name": "@nemesisjs/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "NemesisJS CLI - scaffolding and code generation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "nemesis": "./bin/nemesis.ts"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun && cp -r src/templates dist/templates",
    "test": "bun test"
  },
  "dependencies": {
    "@nemesisjs/common": "workspace:*",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "latest",
    "@types/mustache": "^4.2.5"
  }
}
